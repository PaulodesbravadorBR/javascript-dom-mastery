# 🔄 Dynamic Button Toggling (Exercício 28)

Este projeto foca na interação entre múltiplos elementos do DOM para controlar estados de interface.

### ✨ Funcionalidades:
* **Controle Externo**: Um botão mestre que habilita ou desabilita um terceiro botão ("Iniciar").
* **Manipulação de Atributos**:
    * `setAttribute("disabled", "disabled")`: Bloqueia o elemento.
    * `removeAttribute("disabled")`: Libera o elemento para interação.

### 🛠️ Aprendizado Técnico:
Diferente dos exercícios anteriores onde apenas verificávamos o estado, aqui aplicamos a lógica em tempo real baseada em eventos de clique (`addEventListener`), simulando comportamentos reais de sistemas de gerenciamento (Dashboards).